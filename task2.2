#include <iostream>
#include <cmath>
using namespace std;

int main() {
	setlocale(LC_ALL,"rus");
	double a,x,w;
	cout<<"Ââåäèòå äâå ïåðåìåííûå";
	cin>>a>>x;
	if (abs(x)<1){
		w = a*log(abs(x));
	}
	else{
		w = sqrt(a-pow(x));
	}
	cout<<w;
	return 0;
}
